{{define "csharp_csproj"}}<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>{{.TargetFramework}}</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>{{.RootNamespace}}</RootNamespace>
  </PropertyGroup>
</Project>
{{end}}
